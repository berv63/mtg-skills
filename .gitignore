# mtg-checklist-needs no longer requires a fixed project-folder location (see README.md "The
# mtg-checklist-needs working directory") — but if the user happens to run it while working
# directly in this repo's root, don't let its per-run ownership data or rendered output get
# committed by accident.
/owned/
/Owned/
/code/

# Python bytecode caches from running the stdlib scripts locally.
__pycache__/
